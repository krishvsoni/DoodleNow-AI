name: doodle_for_gdsc
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.1 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  
  perfect_freehand: ^1.0.4
  neopop: ^1.0.2
  speech_to_text: ^6.5.0
  chat_gpt_sdk: ^2.2.5
  flutter_dotenv: ^5.1.0
  flutter_svg: ^2.0.9
  uuid: ^4.2.2
  path_provider: ^2.1.1
  share_plus: ^7.2.1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/
    # - assets/svgs/
